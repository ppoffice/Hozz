import React, { Component, PropTypes } from 'react';

class Editor extends Component {
    constructor (props) {
        super(props);
    }

    render () {
        return (<div className="app-editor">Editor</div>);
    }
}

export default Editor;