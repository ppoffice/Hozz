import React, { Component, PropTypes } from 'react';

class Settings extends Component {
    static displayName = "Settings";

    constructor (props) {
        super(props);
    }

    render () {
        return (<div className="app-page-settings">settings</div>);
    }
}

export default Settings;